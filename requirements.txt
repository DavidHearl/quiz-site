Django==3.2.21
gunicorn==21.2.0
psycopg2-binary==2.9.9
dj-database-url==0.5.0
whitenoise==6.7.0
python-dotenv==1.0.0
Pillow==10.4.0

# only keep these if your code actually uses them:
django-allauth==0.58.1
django-storages==1.13.1
cloudinary==1.34.0

# if your code parses XML or relies on it:
lxml==4.9.4

# if you actually use pandas/numpy in server code (not desktop tools):
numpy==1.24.4
pandas==1.4.2
django-widget-tweaks>=1.4.12
python-Levenshtein>=0.25
